<Window x:Class="PFXManager.App.Views.Dialogs.ConfirmDeleteDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Height="200" Width="420"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Loaded="Window_Loaded">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="{Binding Message}"
                   TextWrapping="Wrap"
                   FontSize="14"
                   VerticalAlignment="Center" />

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button x:Name="CancelButton" Content="{Binding CancelText}" IsCancel="True" IsDefault="True"
                    Width="140" Height="32" Margin="0,0,8,0" Click="CancelButton_Click" />
            <Button x:Name="DeleteButton" Content="{Binding DeleteText}"
                    Width="160" Height="32" Background="#C0392B" Foreground="White"
                    Click="DeleteButton_Click" />
        </StackPanel>
    </Grid>
</Window>
