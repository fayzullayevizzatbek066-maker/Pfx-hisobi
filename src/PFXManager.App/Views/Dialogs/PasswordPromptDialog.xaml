<Window x:Class="PFXManager.App.Views.Dialogs.PasswordPromptDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Height="180" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Loaded="Window_Loaded">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" x:Name="PromptText" TextWrapping="Wrap" FontSize="13" Margin="0,0,0,12" />

        <!--
            Deliberate security choice (section 11): a standard PasswordBox, never bound via
            data-binding (PasswordBox.Password cannot be safely data-bound), read only once at
            submit time, never persisted, never logged, never copied to the clipboard automatically.
        -->
        <PasswordBox x:Name="PasswordInput" Grid.Row="1" Height="30" KeyDown="PasswordInput_KeyDown" />

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Bekor qilish" IsCancel="True" Width="120" Height="32" Margin="0,0,8,0" Click="CancelButton_Click" />
            <Button x:Name="OkButton" Content="OK" IsDefault="True" Width="100" Height="32" Click="OkButton_Click" />
        </StackPanel>
    </Grid>
</Window>
